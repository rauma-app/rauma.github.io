import React from 'react';
import { SpecialCategoryList } from './SpecialCategoryList';

export default function TakeOverKPRPage() {
  return (
    <SpecialCategoryList
      type="take_over_kpr"
      title="Take Over KPR"
      intro="Rumah yang masih dalam cicilan KPR dan dioper-alihkan (take over) ke pembeli baru. Hubungi penjual lewat kontak di tiap listing untuk detail sisa cicilan dan proses take over ke bank terkait."
      seoDescription="Cari rumah take over KPR di seluruh Indonesia — lanjutkan cicilan KPR rumah yang sudah berjalan dengan harga lebih terjangkau. Temukan listing take over KPR terbaru di Rauma."
      path="/take-over-kpr"
    />
  );
}
